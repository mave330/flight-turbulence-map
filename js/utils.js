export const clamp=(v,min,max)=>Math.min(max,Math.max(min,v));
export function debounce(fn,wait=120){let t;return(...args)=>{clearTimeout(t);t=setTimeout(()=>fn(...args),wait)}}
export function normalize(value){return String(value??"").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g," ").trim()}
export function escapeHtml(s){return String(s??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]))}
export function haversineNm(a,b){const R=3440.065,toRad=x=>x*Math.PI/180;const dLat=toRad(b.lat-a.lat),dLon=toRad(b.lon-a.lon);const h=Math.sin(dLat/2)**2+Math.cos(toRad(a.lat))*Math.cos(toRad(b.lat))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(h))}
export function greatCircle(a,b,count){const rad=x=>x*Math.PI/180,deg=x=>x*180/Math.PI;const p1=[rad(a.lat),rad(a.lon)],p2=[rad(b.lat),rad(b.lon)];const d=2*Math.asin(Math.sqrt(Math.sin((p2[0]-p1[0])/2)**2+Math.cos(p1[0])*Math.cos(p2[0])*Math.sin((p2[1]-p1[1])/2)**2));if(d<1e-9)return [{lat:a.lat,lon:a.lon,distanceNm:0}];return Array.from({length:count},(_,i)=>{const f=i/(count-1),A=Math.sin((1-f)*d)/Math.sin(d),B=Math.sin(f*d)/Math.sin(d);const x=A*Math.cos(p1[0])*Math.cos(p1[1])+B*Math.cos(p2[0])*Math.cos(p2[1]);const y=A*Math.cos(p1[0])*Math.sin(p1[1])+B*Math.cos(p2[0])*Math.sin(p2[1]);const z=A*Math.sin(p1[0])+B*Math.sin(p2[0]);return{lat:deg(Math.atan2(z,Math.sqrt(x*x+y*y))),lon:deg(Math.atan2(y,x)),distanceNm:haversineNm(a,b)*f}})}
export function nearestIsoHour(date=new Date()){const d=new Date(date);d.setUTCMinutes(0,0,0);if(date.getUTCMinutes()>=30)d.setUTCHours(d.getUTCHours()+1);return d.toISOString().slice(0,13)+":00"}
