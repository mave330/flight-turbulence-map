const assert=require('assert');const F=require('../js/core.js');const airports=F.prepare([{iata:'CDG',icao:'LFPG',name:'Paris Charles de Gaulle',city:'Paris',country:'FR',lat:49.0097,lon:2.5479},{iata:'JFK',icao:'KJFK',name:'John F Kennedy',city:'New York',country:'US',lat:40.6413,lon:-73.7781}]);assert.equal(F.search(airports,'lfpg')[0].iata,'CDG');assert.equal(F.search(airports,'New York')[0].iata,'JFK');const d=F.distance(airports[0],airports[1]);assert(d>3000&&d<3200);const r=F.route(airports[0],airports[1],25);assert.equal(r.length,25);assert(Math.abs(r[0].lat-airports[0].lat)<1e-6);assert.equal(F.severity(10)[0],'Calm');assert.equal(F.severity(80)[0],'Severe');console.log('core tests: PASS');
